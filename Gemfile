clear_sources

source "http://gemcutter.org"

only :runtime do
  gem 'rack',    '>= 1.0'
end

only :test do
  gem 'shotgun', '>= 0.3',   '< 1.0'
  gem 'rack-test', '>= 0.3.0'
  gem 'builder'
  gem 'erubis'
  gem 'haml'
end

disable_system_gems
